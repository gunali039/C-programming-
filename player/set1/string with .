#include <stdio.h>

int main(void) {
	int i,n;
	char a[10];
	scanf("%s",a);
	n=strlen(a);
	for(i=0;i<=n;i++)
	{
		if(a[i]!='\0')
		{
			printf("%c",a[i]);
		}
		else
		{
			printf(".");
		}
	}
	// your code goes here
	return 0;
}
